let x=2;
typeof(x); //number
console.log(typeof(x));
let y="Hello";
typeof(y); //string
console.log(typeof(y));
 
let z=true;
typeof(z); //boolean
console.log(typeof(z));

let a;
typeof(a); //undefined
console

let b=null;
typeof(b); //object (this is a known quirk in JavaScript)
console.log(typeof(b));

let marks=[90, 80, 70];
typeof(marks); 
console.log(typeof(marks)); //object (arrays are a type of object in JavaScript)   